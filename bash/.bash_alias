#!/bin/bash

# open ~/.bashrc in using the default editor specified in $EDITOR
alias ec="$EDITOR $HOME/.bashrc"

# source ~/.bashrc
alias sc="source $HOME/.bashrc"

if [[ $OSTYPE == darwin* ]]; then
  source "$HOME/.macos_alias"
fi